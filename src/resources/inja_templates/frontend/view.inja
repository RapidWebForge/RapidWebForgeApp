import React from "react";

export default function {{ component }}() {
  return (
    <div>
      {% for comp in components %}
        {{ render_component(comp) }}
      {% endfor %}
    </div>
  );
}
